server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Shuraviraj/microservice-tutorial-config
          clone-on-start: true
